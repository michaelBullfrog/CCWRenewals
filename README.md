# Cisco Renewal Pull

Scheduled Python job that pulls Cisco Modern Commerce subscription renewal data for US and Canada, exports a combined CSV, and uploads it to OneDrive for Power BI.

## Render

Build command:
pip install -r requirements.txt

Start command:
python cisco_renewal_pull.py
